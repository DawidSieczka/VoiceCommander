<Window x:Class="VoiceTypePL.App.Overlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowActivated="False"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="Manual"
        FontFamily="Segoe UI">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />

        <!-- Przycisk-„pigułka" w dwóch wariantach: akcent (Zatwierdź) i neutralny. -->
        <Style x:Key="PillButton" TargetType="Button">
            <Setter Property="Foreground" Value="#F2F2F2" />
            <Setter Property="Background" Value="#3A3A3D" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Margin" Value="6,0,0,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource PillButton}">
            <Setter Property="Background" Value="#4CAF50" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
    </Window.Resources>

    <!-- Cień + zaokrąglona karta. Margines robi miejsce na cień (okno jest przezroczyste). -->
    <Border Width="380" Margin="14" CornerRadius="12" Background="#F21E1E1E"
            BorderBrush="#33FFFFFF" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="3" Opacity="0.5" Color="#000000" />
        </Border.Effect>

        <StackPanel Margin="16,14">
            <!-- Nagłówek: tytuł + licznik „+N oczekujące". -->
            <Grid>
                <TextBlock Text="VoiceType PL" Foreground="#8CFFFFFF" FontSize="11"
                           FontWeight="SemiBold" VerticalAlignment="Center" />
                <Border HorizontalAlignment="Right" CornerRadius="9" Background="#5533AAFF"
                        Padding="8,2" Visibility="{Binding HasPending, Converter={StaticResource BoolToVisibility}}">
                    <TextBlock Text="{Binding PendingText}" Foreground="#DFFFFFFF" FontSize="11" />
                </Border>
            </Grid>

            <!-- Transkrypcja (edytowalna). -->
            <TextBox x:Name="TranscriptBox"
                     Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,10,0,0"
                     MinHeight="46"
                     TextWrapping="Wrap"
                     AcceptsReturn="False"
                     Background="#2A2A2D" Foreground="#F5F5F5" CaretBrush="#F5F5F5"
                     BorderBrush="#33FFFFFF" BorderThickness="1" Padding="8,6"
                     FontSize="14" VerticalContentAlignment="Center" />

            <!-- Akcje. -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button x:Name="RerecordButton" Style="{StaticResource PillButton}" Content="Nagraj ponownie" />
                <Button x:Name="RejectButton" Style="{StaticResource PillButton}" Content="Odrzuć (Esc)" />
                <Button x:Name="ConfirmButton" Style="{StaticResource AccentButton}" Content="Zatwierdź (Enter)" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
